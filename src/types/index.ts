export type Skill = {
  label: string;
  image: string;
  url: string;
};

export type Project = {
  title: string;
  description: string;
  image: string;
  demo?: string;
  deployedUrl?: string;
};
